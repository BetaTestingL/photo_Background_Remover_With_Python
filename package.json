{
  "name": "photo-background-remover-e2e",
  "version": "1.0.0",
  "description": "Playwright end‑to‑end tests for the Photo Background Remover application.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "install": "npm ci"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  },
  "engines": {
    "node": ">=18"
  }
}