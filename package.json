{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "test": "npm run lint && jest",
    "lint": "eslint .",
    "test:e2e": "jest"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "@playwright/test": "^1.40.0",
    "jest-playwright-preset": "^3.0.0"
  }
}
===END===